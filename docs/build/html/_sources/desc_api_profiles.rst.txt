Profiles functions
===================

Here we find the Profiles :

For all, in list presentation
-----------------------------

The `index` function in the `profiles.views` module:

.. autofunction:: profiles.views.index

For an individual, in detail
-----------------------------

The `letting` function in the `profiles.views` module:

.. autofunction:: profiles.views.profile